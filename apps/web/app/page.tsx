export default function Dashboard() {
  return (
    <main className="p-8">
      <h1 className="text-2xl font-semibold">Dashboard</h1>
      <p className="mt-2 text-slate-600">
        Resumo inicial (leads, oportunidades, clientes, alertas) – conectar aos endpoints conforme
        specs.
      </p>
    </main>
  );
}
